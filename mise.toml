[tools]
dotnet = "10.0"

[tasks.restore]
description = "Restore dependencies"
run = "dotnet restore"

[tasks.tool-restore]
description = "Restore local dotnet tools"
run = "dotnet tool restore"

[tasks.build]
description = "Build the solution"
run = "dotnet build"
depends = ["restore"]

[tasks.test]
description = "Run tests with coverage"
run = """
dotnet test --project tests/UnionConfig.Tests \
  --coverage \
  --coverage-output-format cobertura \
  --coverage-output "$PWD/coverage/UnionConfig/coverage.cobertura.xml"
"""
depends = ["build"]

[tasks.example]
description = "Run the example app"
run = "dotnet run --project examples/ExampleApp"
depends = ["build"]

[tasks.example-build]
description = "Build the example app (for CI)"
run = "dotnet build examples/ExampleApp --warnaserror"
depends = ["build"]

[tasks.format]
description = "Format code with Fantomas"
run = "dotnet fantomas src/ tests/ examples/"
depends = ["tool-restore"]

[tasks.format-check]
description = "Check formatting without making changes"
run = "dotnet fantomas src/ tests/ examples/ --check"
depends = ["tool-restore"]

[tasks.lint]
description = "Run FSharpLint"
run = "dotnet fsharplint lint UnionConfig.slnx"
depends = ["build", "tool-restore"]

[tasks.coverage-check]
description = "Check per-file coverage meets minimum threshold"
run = "(cd coverage/UnionConfig && dotnet tool run coverageratchet check ../../coverage-ratchet-UnionConfig.json)"
depends = ["test"]

[tasks.coverage-ratchet]
description = "Ratchet coverage thresholds to current values"
run = "(cd coverage/UnionConfig && dotnet tool run coverageratchet ratchet ../../coverage-ratchet-UnionConfig.json)"
depends = ["test"]

[tasks.sync-docs]
description = "Sync README.md content to docs/index.md"
run = "dotnet tool run syncdocs sync"

[tasks.sync-docs-check]
description = "Check if docs/index.md is in sync with README.md"
run = "dotnet tool run syncdocs check"

[tasks.check]
description = "Run all checks with auto-fix (format, sync-docs, then verify)"
depends = ["format", "sync-docs", "lint", "coverage-check", "docs", "example-build"]

[tasks.ci]
description = "Run all CI checks (no auto-fix)"
depends = ["format-check", "lint", "coverage-check", "sync-docs-check", "docs", "example-build"]

[tasks.pack]
description = "Create NuGet packages"
run = "dotnet pack src/UnionConfig/UnionConfig.fsproj -c Release"
depends = ["build"]

[tasks.release]
description = "Release with automatic semantic versioning"
run = "dotnet tool run fssemantictagger release"
depends = ["ci"]

[tasks.release-alpha]
description = "Release an alpha version"
run = "dotnet tool run fssemantictagger alpha"
depends = ["build"]

[tasks.docs]
description = "Generate API documentation"
run = "dotnet fsdocs build --output output/docs --clean"
depends = ["build", "tool-restore"]

[tasks.docs-watch]
description = "Watch and serve documentation locally"
run = "dotnet fsdocs watch"
depends = ["build", "tool-restore"]
